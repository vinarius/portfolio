version: 0.2
            
phases:
  install:
    runtime-versions:
      nodejs: 12
    commands:
        - npm i -g @angular/cli
  build:
    commands:
      - cd Interface
      - ng b --prod
artifacts:
  files:
      - '/'
  discard-paths: yes
  base-directory: 'dist*'