version: 0.2
            
phases:
  install:
    runtime-versions:
      nodejs: 12
    commands:
      - npm i -g @angular/cli
      - npm i @angular-devkit/build-angular
      - npm i @angular/compiler-cli
      - npm i @angular/compiler
      - npm i @angular/core
  build:
    commands:
      - cd Interface
      - ng b --prod